[FATAL] src/app/page.tsx:139 — "Use Demo Data" fallback dead-ends — `handleUseDemoData()` routes to `aid_opendoor_fixture` / `aid_sonobello_fixture`, but `GET /api/analyze` only special-cases `demo_opendoor` / `demo_sonobello` and otherwise reads KV. On the first real 422/503, the promised recovery path lands on `/preview` with `ANALYSIS_NOT_FOUND`, so the rep cannot recover from a live scrape failure. — fix: use one canonical demo analysis ID scheme end to end and have the server persist/select the fallback payload before navigation.

[FATAL] src/lib/analyze-page.ts:447 — Screenshot contract is a stub — live analyses emit `screenshot.url = /api/screenshot?id=...`, and fixtures point at the same endpoint, but there is no `src/app/api/screenshot/route.ts` and no screenshot bytes are ever stored in KV. The API advertises a capability the app does not implement, so screenshot refresh/share is broken by design. — fix: add `GET /api/screenshot`, persist the PNG by `analysisId`, and only return screenshot URLs that resolve.

[FATAL] src/lib/gemini.ts:5 — Production credentials are hardcoded in source — the Vertex API key and project identifier are embedded directly in the repo instead of being loaded from the env contract in the spec. This is both a security leak and a deployment blocker for any environment that is not the original author's GCP project. — fix: remove inline secrets, read the required env vars only, and fail fast when credentials are missing.

[FATAL] src/app/api/analyze/route.ts:84 — Successful POST responses do not honor the required KV persistence invariant — demo URL success paths return 200 without writing to KV, and the live path swallows `kv.set()` failures and still returns success. That breaks the spec's core promise that every 200 response is persisted verbatim before return and makes refresh/share behavior nondeterministic. — fix: persist every success payload before responding and return `503 KV_WRITE_ERROR` if storage fails.

[MAJOR] src/app/page.tsx:72 — The `/analyze?url=...` loading route does not exist in practice — the home page posts directly to `/api/analyze` and then jumps straight to `/preview`, so there is no dedicated analysis page, no cancel action, and no way to satisfy the documented `/analyze` URL contract or acceptance tests A3/A5. — fix: implement `src/app/analyze/page.tsx` and route all submits/demo clicks through it.

[MAJOR] src/app/preview/page.tsx:154 — The preview misses the entire L1/L2/L3 reveal model — there are no "Field Detection", "AI Copy", or "Performance" tabs, no confidence badges, no field mapping list, no copy comparison pane, and no performance visualization. The demo script in the spec cannot actually be delivered from this UI. — fix: add the tabbed preview shell and render the required content for each layer.

[MAJOR] src/app/preview/page.tsx:408 — Field editing and copy regeneration are not implemented — the generated form is a static render with no "Edit" controls, no modal, no client-side validation, no screenshot zoom, and no regenerate-copy action backed by `/api/generate`. This fails the explicit UI interaction requirements C2-C8. — fix: add field edit controls + modal state, implement validation/save/cancel rules, add screenshot zoom, and build `POST /api/generate`.

[MAJOR] src/lib/analyze-page.ts:242 — Live journey crawling stops after step 1 whenever that first page already has fields — the CTA click path only runs when `step1Fields.length === 0`, so common flows that collect some data on the first page and more data later are flattened into a single-step journey. That breaks the core 3P-vs-1P story for real URLs. — fix: continue scoring and following the primary CTA even when the initial page already contains fields.

[MAJOR] src/lib/analyze-page.ts:152 — Scrape time budgets are far below the spec and below real-world page behavior — navigation is capped at 1.5s and screenshots at 0.5s, even though the spec budgets 2.0s for screenshots and expects best-effort JS-rendered page handling. In practice this will produce partial DOMs, failed screenshots, and brittle demos on normal advertiser sites. — fix: align the step budgets with the spec and wait on targeted selectors instead of racing everything at sub-second thresholds.

[MAJOR] src/lib/analyze-page.ts:167 — Anti-bot handling is nowhere near demo-safe — the only mitigation is a user-agent override and a naive consent clicker. There is no challenge detection, no request interception strategy, no modal/iframe robustness, and no retry path, so live sites that use bot protection are likely to fail in front of the customer. — fix: add explicit bot-detection handling, smarter waits/interactions, and deterministic fallback classification when protection is encountered.

[MAJOR] src/app/api/analyze/route.ts:191 — The error contract is incomplete and frequently mislabeled — the route never returns `NO_FORM_DETECTED`, `PRIMARY_FORM_UNCERTAIN`, `KV_WRITE_ERROR`, or 429 `RATE_LIMITED`, and many LLM failures will be misreported as `SCRAPING_BLOCKED` because the mapper only looks for the substring `"LLM"`. This breaks both the API contract and the intended client fallback behavior. — fix: propagate typed errors from scraper/LLM/KV layers and map them to the exact status/code matrix in the spec.

[MAJOR] src/app/api/analyze/route.ts:33 — Rate limiting and demo bypass rules are missing entirely — there is no per-IP KV counter, no `X-RateLimit-*` or `Retry-After` headers, and no `FORCE_DEMO_MODE` / `RATE_LIMIT_EXEMPT_IPS` behavior. The spec makes these part of the API surface, especially for live demo stability. — fix: implement the specified KV-backed rate limiter and emit the required headers on all POST responses.

[MAJOR] src/app/bonus/page.tsx:108 — The retargeting page hides the required simulated-data disclosure for live analyses — `isSimulatedData` only tracks whether the whole analysis was synthetic, but the retargeting metrics are simulated even on live analyses. That means the bonus page drops the "SIMULATED DEMO DATA" banner in exactly the scenario where the spec says it must be prominent. — fix: track retargeting simulation separately and always show the required banner/label for the bonus flow.

[MINOR] src/app/preview/page.tsx:223 — The left pane never renders the promised form highlight overlay — the page ignores `formBoundingBox` and falls back to generic white-box mockups for most steps instead of showing an actual landing-page screenshot with a highlighted form. That makes the 3P side look synthetic instead of persuasive. — fix: overlay the bounding box on a real screenshot and capture/store the screenshots for the steps you expose.

[MINOR] src/app/globals.css:22 — Typography regresses to default Arial — the layout loads Geist, but the global CSS overrides the body font with `Arial, Helvetica, sans-serif`, which makes the product read like a quick prototype rather than a polished exec demo. — fix: use the configured font variables as the global body font and define a deliberate type scale.

[MINOR] package.json:9 — Release hygiene is already red because lint fails — `npm run lint` exits non-zero on the current tree due to JSX entity issues in the preview and bonus pages. Shipping a C-level demo with a failing lint gate is a sign the implementation has not been through a basic quality pass. — fix: resolve the current ESLint errors and keep the lint script green before calling the implementation ready.

4 FATAL, 9 MAJOR, 3 MINOR
