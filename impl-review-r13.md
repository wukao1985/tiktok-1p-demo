[MAJOR] src/lib/analyze-page.ts:449 - Navigation timeouts can still be mislabeled as `PAGE_NOT_FOUND` - `getBrowserErrorPageFailure()` unconditionally converts `about:blank` into `PAGE_NOT_FOUND`, and both the initial `page.goto()` timeout path (`src/lib/analyze-page.ts:513-516`) and the post-click timeout/null-response path call this helper before the request-level timeout fallback can take over. In Chromium, a slow but valid navigation can remain on `about:blank` until the first response commits, so real timeout cases can still return `422 PAGE_NOT_FOUND` instead of timing out cleanly. - fix: only classify `about:blank` as `PAGE_NOT_FOUND` when browser error text/status signals also confirm a DNS/HTTP failure; otherwise treat it as an unresolved timeout and let the higher-level timeout handling decide.
[MINOR] src/lib/demo-data.ts:119 - Sono Bello demo overlay still misses part of the shipped form screenshot - The preview overlay is now rendered correctly (`src/app/preview/page.tsx:596-600`), but the stored `SONO_BELLO_DEMO.formBoundingBox` only covers the left two columns of `public/sonobello-step1.png`; the visible email/location column on the right sits outside the highlighted region. That leaves one of the two shipped demo fixtures visually inaccurate even though the overlay mechanism itself is present. - fix: recapture/recompute the Sono Bello bounding box against the checked-in PNG so it encloses the full step-1 form.

0 FATAL, 1 MAJOR, 1 MINOR.
