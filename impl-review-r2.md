[FATAL] src/lib/gemini.ts:12 — Hardcoded Vertex credential is still shipped in source — The code now checks env vars first, but it still embeds a real API key as the fallback default and injects it into the request URL. That leaves the original secret-exposure issue intact for anyone with source access, even though project/location fallbacks are harmless. — fix: remove the inline API key from the repo and make demo/no-env behavior use mocked analysis responses or a non-secret local stub instead of a shipped production credential.

[FATAL] src/app/api/analyze/route.ts:93 — Successful `POST /api/analyze` responses still do not guarantee KV persistence — `storeDemoPayload()` silently swallows both analysis and screenshot write failures, and `storeLiveArtifacts()` only logs KV errors before the 200 success paths return. The spec requires every 200 response to be durably persisted before return, so refresh/share behavior is still nondeterministic whenever KV is unavailable. — fix: make all success-path writes mandatory and return `503 { success:false, error:{ code:"KV_WRITE_ERROR" } }` on any persistence failure.

[MAJOR] src/app/page.tsx:56 — The primary entry flow still bypasses `/analyze?url=...` — `handleAnalyze()` still posts directly to `/api/analyze`, and both `handleSubmit()` and `handleDemoSelect()` still call it. The new `/analyze` page exists, but the main home-page journey never uses it, so the loading-route contract and acceptance flow A3 are still broken in the default UX. — fix: have the home page navigate to `/analyze?url=${encodeURIComponent(url)}` and let `/analyze` own the POST, progress UI, cancel action, and fallback button.

[MAJOR] src/app/api/analyze/route.ts:99 — Demo screenshot hydration still resolves to a transparent placeholder — `storeDemoPayload()` writes `PLACEHOLDER_PNG_BASE64` for the demo analysis IDs, while the demo payloads and journey steps only reference `/api/screenshot?id=demo_*`. That means the flagship Opendoor/Sono Bello previews hydrate a blank 1x1 PNG instead of the checked-in assets under `public/`, which materially weakens the sales demo on refresh/share. — fix: seed KV from the real demo PNGs or point the demo payload directly at static screenshot asset URLs.

[MAJOR] src/lib/analyze-page.ts:461 — `PRIMARY_FORM_UNCERTAIN` is still unreachable — The route now maps `PRIMARY_FORM_UNCERTAIN`, but the scraper never throws it. `analyzePage()` only throws `bot blocked` and `NO_FORM_DETECTED`, and there is still no primary-form scoring / top-two-delta logic anywhere in the scrape pipeline, so ambiguous multi-form pages are still forced into a guessed success path. — fix: implement the spec’d form scoring threshold and throw `PRIMARY_FORM_UNCERTAIN` when the top candidate is too weak or too close to the runner-up.

[MAJOR] src/lib/analyze-page.ts:58 — Scraping-block detection is still keyword-only — `detectBotProtection()` only scans `document.title` and `body.innerText` for a small keyword list. Common challenge flows rendered in iframes, canvases, or JS interstitials will bypass this check and degrade into timeouts or bad extraction instead of a clean `SCRAPING_BLOCKED` response with fallback UI. — fix: add explicit challenge/interstitial detection beyond page text and fail fast with typed blocked errors when those patterns are present.

[MINOR] src/app/preview/page.tsx:528 — The left pane still ignores `formBoundingBox` — The preview now shows screenshots and tabs, but the screenshot is rendered raw and there is still no overlay using `data.formBoundingBox`. The spec explicitly calls for the original form to be highlighted, so the 3P side still looks flatter than intended. — fix: render the bounding-box overlay on top of the screenshot container for the active step.

2 FATAL, 4 MAJOR, 1 MINOR
