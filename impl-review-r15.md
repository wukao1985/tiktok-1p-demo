[MAJOR] src/lib/analyze-page.ts:770 - Non-form primary-form scoring still loses the top-two rubric - Non-form containers are located with `getNonFormContainerSelectionScore()`, but the final ranking and `PRIMARY_FORM_UNCERTAIN` decision switch back to `getFieldScore()` only. That drops the semantic/action bonuses that were used to identify meaningful non-form containers, so two div/section-based candidates can collapse to the same score and incorrectly return `422 PRIMARY_FORM_UNCERTAIN` or select the wrong container. - fix: carry the non-form selection score through to `scoredCandidates` and use one consistent scoring function for candidate ranking, final selection, and the top-two-delta check.
[MAJOR] src/lib/analyze-page.ts:423 - Inline CAPTCHA widgets still trigger `SCRAPING_BLOCKED` - `detectBotProtection()` returns blocked when any selector matches, and `BOT_BLOCK_SELECTORS` includes generic inline widget markers like `[data-sitekey]`, `.cf-turnstile`, and `name="cf-turnstile-response"`. Many legitimate lead forms ship invisible reCAPTCHA/Turnstile controls before submit, so those pages are rejected as bot challenges even when the form is otherwise usable. - fix: treat widget selectors as supporting evidence only, and require a corroborating interstitial signal such as challenge copy/title, a full-page blocker, or a 401/403/429 response before returning `SCRAPING_BLOCKED`.
[MAJOR] src/lib/analyze-page.ts:366 - Connection failures are still mislabeled as `PAGE_NOT_FOUND` - `normalizeNavigationError()` maps `ERR_CONNECTION_REFUSED`, `ERR_ADDRESS_UNREACHABLE`, and `ERR_CONNECTION_CLOSED` to `PAGE_NOT_FOUND` via `NOT_FOUND_MESSAGE_PATTERNS`, even though the API contract describes that code as "404 or DNS failure". Those transport failures are transient/non-404 navigation errors, so callers receive the wrong non-retryable classification. - fix: limit `PAGE_NOT_FOUND` mapping to 404 and hostname-resolution failures, and route other connection-level navigation errors to a distinct retryable network failure or blocked classification.
[MINOR] src/lib/demo-data.ts:318 - Opendoor demo overlay coordinates still do not match the shipped screenshot - Preview now renders `formBoundingBox` directly on the screenshot (`src/app/preview/page.tsx:596`), but the stored `OPENDOOR_DEMO.formBoundingBox` (`x:95, y:560, width:390, height:75`) sits below the actual "Enter home address" input in `public/opendoor-step1.png`. The left pane therefore highlights empty space in the flagship demo flow. - fix: recapture or recompute the Opendoor bounding box against the checked-in PNG so it encloses the actual address form.

0 FATAL, 3 MAJOR, 1 MINOR.
