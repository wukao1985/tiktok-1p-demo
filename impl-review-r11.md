[MAJOR] src/lib/gemini.ts:645 - Generic Gemini transport errors still downgrade to `422 SCRAPING_BLOCKED` - `callVertex()` rethrows non-timeout `fetch()` failures unchanged. In Node these surface as `TypeError: fetch failed`, which reaches `mapAnalyzeError()` without any `llm|vertex|gemini` marker, so the `503 LLM_ERROR` branch in `src/app/api/analyze/route.ts:424` is skipped and the default `src/app/api/analyze/route.ts:436` branch returns `422 SCRAPING_BLOCKED` instead. - fix: wrap every non-timeout Gemini transport failure in a dedicated LLM/Gemini error class or prefix before rethrowing, and map that explicit signal to `503 LLM_ERROR`.

[MAJOR] src/lib/gemini.ts:732 - Copy-generation Gemini payload is still unvalidated after JSON parsing - `analyzeWithGemini()` validates parsed JSON with `validateGeminiResponse()`, but `generateCopyWithGemini()` trusts `parseJsonResponse<GenerateResponse>(text)` and returns fields directly. A syntactically valid but incomplete copy payload therefore flows through `src/app/api/generate/route.ts:119` and can still be returned from `src/app/api/generate/route.ts:138` as `200 success`, with missing headline/CTA/benefits values that will break preview regeneration instead of throwing `GeminiFormatError`. - fix: add runtime schema validation for the generate-copy response and throw `GeminiFormatError` (or a dedicated format error) before returning malformed data.

0 FATAL, 2 MAJOR, 0 MINOR.
