Re-verified the current implementation against `SPEC.md` and all prior findings in `impl-review-r1.md` through `impl-review-r6.md`. Prior issues not listed below appear closed. `npm run lint` and `npm run build` both pass on the current tree, but the following spec gaps remain.

[MAJOR] Section 6: Analysis payloads are still not persisted verbatim to KV — `src/app/api/analyze/route.ts:156-166` still writes `analysis:{analysisId}` via `JSON.stringify(data)`, even though Section 6 says the server must persist `data` verbatim and acceptance test A6b compares `GET /api/analyze?aid=...` against the raw KV record. The read path papers over this by parsing strings back into objects, but the stored record itself is still not the canonical `AnalyzeResponseData` shape required by the spec. — fix: write the `AnalyzeResponseData` object directly to KV instead of a JSON string, and keep `GET /api/analyze` returning that exact stored object.

[MAJOR] Section 10: Primary-form selection is still not implemented per the spec heuristic — `src/lib/analyze-page.ts:226-313` still extracts every `input`, `select`, and `textarea` in the document with no `FIELD_SELECTORS` / `IGNORED_SELECTORS` filtering and no DOM-level `selectPrimaryForm()` scoring pass. `PRIMARY_FORM_UNCERTAIN` is now thrown only from Gemini self-reported scores at `src/lib/analyze-page.ts:591-595`, which means header/search/newsletter fields can still pollute extraction and ambiguous multi-form pages still are not resolved by the deterministic Section 10 rubric (`score < 60` or top-two delta `< 15`). This means the earlier `PRIMARY_FORM_UNCERTAIN` issue is not fully closed. — fix: implement the Section 10 form-selection heuristic before extraction, ignore non-lead-capture inputs per spec, and raise `PRIMARY_FORM_UNCERTAIN` from that DOM scoring result.

[MAJOR] Section 15: Confidence badge thresholds still fail A10 and A11 — `src/app/preview/page.tsx:59-68` maps `confidence >= 0.8` to green and `>= 0.5` to yellow. The acceptance criteria explicitly require `0.98 -> bg-green-500`, `0.85 -> bg-yellow-500`, and `0.75 -> bg-red-500`, so the current implementation still renders `0.85` as green and `0.75` as yellow. The Round 6 confidence-badge issue therefore remains open. — fix: change the threshold mapping to the spec’s acceptance buckets so 0.85 renders yellow and 0.75 renders red.

Count: 0 FATAL, 3 MAJOR, 0 MINOR
