[MAJOR] src/lib/analyze-page.ts:1072 - Post-click failure normalization is still incomplete - `waitForNavigation()` is now armed before the click, but the post-click path still does not normalize every outcome. `normalizePostClickNavigationOutcome()` only handles explicit `response` / `error` results, and no post-click branch calls `getBrowserErrorPageFailure()`, so timeout or null-response navigations that land on `chrome-error://` / DNS-style failure documents can still fall through instead of returning `422 PAGE_NOT_FOUND`. Separately, if `element.click()` and the fallback `node.click()` both throw after navigation has already started, `clickElementHandle()` exits through the outer catch without ever awaiting `navigationOutcomePromise`, so a pending 403/404/challenge result can still be dropped as a generic journey break. - fix: always await and normalize `navigationOutcomePromise` in a `finally`/catch-safe path, and run browser-error-page detection after timeout/null-response outcomes before continuing.

[MAJOR] src/lib/demo-data.ts:119 - Demo form overlays still do not match the shipped step-1 screenshots - `primaryFormStepNumber` now points at step 1 in both fixtures (`src/lib/demo-data.ts:125`, `src/lib/demo-data.ts:401`), and preview renders `data.formBoundingBox` directly on that step (`src/app/preview/page.tsx:431-585`), but the stored boxes are stale. Visual verification against `public/sonobello-step1.png` and `public/opendoor-step1.png` shows the Sono Bello box covering a large blank/right-side region instead of the visible form, while the Opendoor box sits across the hero copy instead of the address input (`src/lib/demo-data.ts:395-400`). - fix: recapture/recompute both `formBoundingBox` values from the current step-1 assets, or replace the assets so the stored coordinates and overlay line up.

0 FATAL, 2 MAJOR, 0 MINOR
