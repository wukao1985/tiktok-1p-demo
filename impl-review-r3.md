[FATAL] src/app/api/analyze/route.ts:96 — Demo POST→KV→GET parity is still broken — `storeDemoPayload()` still swallows KV write failures, and `GET /api/analyze?aid=demo_*` still bypasses KV entirely at lines 428-444 and returns hard-coded fixture objects instead of the exact payload returned by `POST /api/analyze`. The primary demo flows therefore still violate the core persistence invariant: refresh/share can return a different `landingPageUrl`, `createdAt`, and screenshot contract than the original 200 response. — fix: make demo writes mandatory, return `503 KV_WRITE_ERROR` on any demo write failure, and hydrate demo IDs from KV before any fixture fallback.

[FATAL] src/lib/demo-data.ts:10 — Flagship demo screenshots still collapse to blank placeholders — The shipped demo payloads and step data still point at `/api/screenshot?id=demo_*`, but no code stores `screenshot:demo_*`, and `src/app/api/screenshot/route.ts` returns a 200 transparent 1x1 PNG when those keys are missing. On the two preset sales-demo journeys, the left-hand 3P screenshot pane is still effectively blank on refresh/share/timeout fallback instead of showing the checked-in assets in `public/`. — fix: point demo/root/journey screenshots at static assets like `/opendoor-step1.png` and `/sonobello-step1.png` or seed KV with the real PNGs, and stop masking missing screenshots as successful image responses.

[MAJOR] src/lib/analyze-page.ts:465 — `PRIMARY_FORM_UNCERTAIN` is still functionally unreachable — The guard reads `geminiResult.formConfidence`, but the Gemini prompt/schema in `src/lib/gemini.ts` never request or type that field, so the nullish fallback keeps the score at `100` for normal responses. The top-two delta rule from the spec is also still absent, so ambiguous multi-form pages are still forced into a guessed success path instead of the required `422 PRIMARY_FORM_UNCERTAIN`. — fix: implement actual primary-form scoring with top-two delta handling, or require/validate the score payload from Gemini before using it.

[MAJOR] src/lib/analyze-page.ts:58 — Bot/challenge handling still degrades into partial “success” results — Detection is still limited to a small keyword scan over title/body text, and when a block page appears after CTA navigation the `bot blocked` error thrown inside the step-2/sub-step branch is swallowed by the broad `catch {}` at lines 404-406. That means real anti-bot challenges can still become half-scraped journeys or bad AI output instead of a clean `SCRAPING_BLOCKED` response with demo fallback. — fix: add explicit challenge/interstitial detection beyond text, and propagate typed scraper-block errors instead of swallowing them in the CTA flow.

[MAJOR] src/lib/analyze-page.ts:238 — `PAGE_NOT_FOUND` is still unlikely to fire for real navigation failures — `page.goto()` failures are still caught and ignored, and successful navigations never inspect the HTTP status code. DNS failures, 404s, and refused connections therefore fall through into blank-page extraction and are likely to surface as `NO_FORM_DETECTED` or a bad partial analysis rather than the route’s `422 PAGE_NOT_FOUND` contract. — fix: capture navigation exceptions/status codes explicitly and raise typed page-not-found errors before continuing scrape logic.

[MAJOR] src/lib/analyze-page.ts:76 — Live screenshots are still viewport-only, not full-page — The scraper still calls `page.screenshot({ fullPage: false })`, even though the spec requires a full-page capture and many lead forms sit below the fold. On live pages, the core 3P visual can still miss the actual form entirely, which weakens both the AI analysis input and the sales-side proof in the preview. — fix: capture full-page screenshots or clip around the detected primary form after scrolling it into view.

[MAJOR] src/app/bonus/page.tsx:184 — The bonus page still shows contradictory lift numbers on the same screen — The Journey Compression hero hardcodes `+47%`, while the stats grid below correctly renders `retargeting.estimatedCtrLift` from the stored analysis payload. The shipped demo data is `+38%` for Opendoor and `+42%` for Sono Bello, so this screen currently presents mutually inconsistent KPIs in front of the customer. — fix: derive the hero metric from the same stored value, or relabel it as a separate benchmark if it is intentionally different.

[MINOR] src/app/preview/page.tsx:528 — The preview still does not render the promised form highlight overlay — The left pane now shows screenshots and zoom controls, but `data.formBoundingBox` is still ignored and the screenshot is rendered raw. The prior R1/R2 issue remains open: the rep still cannot point to a highlighted original form in the 3P screenshot. — fix: render an absolute overlay from `formBoundingBox` on top of the screenshot container and fall back to an explicit placeholder state when the screenshot is unavailable.

2 FATAL, 5 MAJOR, 1 MINOR
