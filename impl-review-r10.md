[MAJOR] src/lib/gemini.ts:465 - Malformed Gemini JSON still does not reliably become `503 LLM_ERROR` - `parseJsonResponse()` only validates JSON syntax, then the transformer assumes the full schema is present. A syntactically valid but incomplete payload can throw generic `TypeError`s from `parsedResult.extractedFields.map(...)`, `parsedResult.brandColors.name`, or `parsedResult.generatedCopy...`, and `mapAnalyzeError()` falls those through to the default `422 SCRAPING_BLOCKED` path in `src/app/api/analyze/route.ts:424`. Separately, a missing/null `formBoundingBox` can flow through to `src/lib/analyze-page.ts:1619`, which fabricates the hard-coded box while keeping `isSimulatedData: false`. Malformed non-timeout Gemini responses therefore still do not consistently propagate as `503 LLM_ERROR`, and some can still surface fake live overlay data. - fix: runtime-validate every required Gemini field before transformation, throw a dedicated Gemini format error for any missing/wrong-type field, and remove the live fallback for Gemini-owned fields such as `formBoundingBox`.

[MINOR] scripts/e2e-test.ts:96 - Production E2E rejects the spec'd timeout-success path - the script asserts `fallbackReason === undefined` and `isSimulatedData === false` for every `200` from `/api/analyze`, so a correct timeout fallback response will be reported as a failure. That means the new timeout contract is still not covered by the regression harness and could regress unnoticed. - fix: explicitly accept the `200` timeout envelope and assert its fallback fields separately from the live-success case.

0 FATAL, 1 MAJOR, 1 MINOR.
