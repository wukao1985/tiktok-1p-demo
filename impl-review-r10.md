[FATAL] src/lib/analyze-page.ts:1403 - Gemini failures are converted into fake live analyses - `analyzeWithGemini(...).catch(...)` swallows every LLM/auth/parse/timeout error and substitutes hard-coded placeholder brand/copy data while the response still ships as `isSimulatedData: false`. That breaks the Section 6/12 contract twice: non-timeout LLM failures should return `503 LLM_ERROR`, and LLM timeouts should return `200` with `fallbackReason:"timeout"` plus simulated data. It also means a broken Gemini integration can present fabricated "live" AI output to the rep. - fix: remove the blanket catch in `analyzePage()`, let non-timeout Gemini errors propagate to the route mapper, and convert only true timeout sentinels into the normal simulated timeout fallback path.

[FATAL] src/app/api/analyze/route.ts:579 - Successful POST responses no longer require persistence - when `storeLiveArtifacts()` times out or throws, the handler catches the failure, strips screenshot URLs with `withoutPersistedArtifactUrls()`, and still returns HTTP 200. The client then navigates purely by `aid`, while `/preview` and `/bonus` immediately rehydrate from `GET /api/analyze?aid=...`; if KV never stored the payload, the analysis cannot be refreshed, shared, or even reopened reliably. - fix: make KV persistence mandatory for every 200 response again and return the spec'd `503 KV_WRITE_ERROR`/finalization failure path instead of a best-effort success.

[FATAL] src/lib/demo-fallback.ts:15 - "Use Demo Data" no longer loads the pre-cached fixtures - the fallback button re-POSTs `/api/analyze` with a demo-looking URL, but `POST /api/analyze` now enters fixture mode only when `rateLimit.forceDemoMode` is true (`src/app/api/analyze/route.ts:510`). On ordinary 422/503 failures this retries live scraping instead of persisting the Opendoor/Sono Bello fixture required by Section 16, so the user-triggered recovery path is no longer deterministic and can fail again immediately. - fix: add an explicit fixture-mode input/path for `persistDemoFixture()` and persist the chosen fixture payload to KV before navigating to `/preview`.

[MAJOR] src/lib/kv-client.ts:220 - The KV fallback is process-local memory, not the durable Vercel KV required by the spec - when `KV_REST_API_URL` is absent, the app silently swaps to a module-level `Map`. That cannot guarantee refresh/share hydration or rate limiting across serverless instances or process restarts, yet the API still reports normal success as if the storage contract were satisfied. - fix: require real KV for spec-compliant success paths, or confine the in-memory store to an explicit local-dev/demo-only mode that never masquerades as durable persistence.

[MAJOR] src/lib/analysis-budget.ts:1 - The 8-second end-to-end timeout contract regressed to 24-27 seconds - `ANALYZE_ROUTE_TIMEOUT_MS` is now `27000`, the internal deadline is `24000`, and the Gemini budget is `4000`, so the server no longer auto-falls back at >8s as required by Sections 2 and 12. A slow live run can now sit in-flight for roughly three times the allowed wall-clock budget instead of returning the seamless timeout fixture. - fix: restore a single <=8.0s wall-clock budget and enforce the timeout fallback at that boundary.

[MAJOR] src/lib/analyze-page.ts:1219 - PRIMARY_FORM_UNCERTAIN is effectively disabled again - the only active call to `extractFieldsFromPage()` passes `{ suppressUncertain: true }`, so low-confidence single forms and close top-two races are silently accepted instead of producing the spec'd `422 PRIMARY_FORM_UNCERTAIN`. That reopens Acceptance A13h even though the scorer still computes the threshold inputs. - fix: propagate the scored-form result through the scrape pipeline and raise `PRIMARY_FORM_UNCERTAIN` whenever `topCandidateScore < 60` or the top-two delta is `< 15`.

[MAJOR] src/lib/analyze-page.ts:1272 - The new journey navigator still truncates real multi-step flows and the final form still collapses to one step - traversal stops as soon as the current step has `>= 2` fields, so partial first-step forms never continue to later qualification/details screens. Even when multiple steps are captured, the final payload almost always returns `bestScrapedStep.fields` (`src/lib/analyze-page.ts:1460`) instead of the deduplicated journey-wide union, which drops later-step required inputs from the generated TikTok form. - fix: continue following the best next-step CTA until no new step is found, then return the deduplicated all-step field set (or Gemini's journey-wide union) rather than one step's fields.

[MAJOR] src/app/preview/page.tsx:431 - Canonical screenshot/highlight handling is hard-coded to step 1, not the step that actually contains the primary form - Gemini is given `bestStepScreenshot` from whichever journey step scored best (`src/lib/analyze-page.ts:1395`), but the stored top-level screenshot metadata is still populated from `scrapedSteps[0]` (`src/lib/analyze-page.ts:1502`) and the overlay only renders when `activeStepData.stepNumber === 1`. On later-step forms, the left pane either highlights the wrong image or shows no highlight at all. - fix: persist which journey step supplied the primary form, store that step's screenshot as the canonical screenshot, and render the overlay against that step instead of assuming step 1.

[MINOR] src/lib/analyze-page.ts:480 - A partial screenshot is reported as a successful full-page capture - `takeScreenshotWithBudget()` falls back from `fullPage: true` to `fullPage: false` on any error but still returns `status: 'ok'`. That violates the full-page screenshot contract and can quietly misalign the preview/overlay without surfacing that the capture was partial. - fix: either mark the screenshot failed when full-page capture fails, or expose a separate partial-capture status instead of reporting success.

[MINOR] src/lib/demo-data.ts:129 - Later demo journey steps all reuse the step-1 PNG - the new `screenshotUrl` fields for Sono Bello and Opendoor point steps 2-N at the same `*-step1.png` asset, so the journey navigator claims to show later pages while rendering the first screenshot repeatedly. That weakens the new multi-step story even in deterministic fixture mode. - fix: add real per-step screenshot assets or leave later-step screenshots unset until accurate assets exist.

3 FATAL, 5 MAJOR, 2 MINOR.
