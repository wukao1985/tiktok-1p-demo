[FATAL] src/app/api/analyze/route.ts:579 - Successful live POST responses still do not require KV persistence - When `storeLiveArtifacts()` times out or throws, the handler catches the failure, strips artifact URLs with `withoutPersistedArtifactUrls(result)`, and still returns HTTP 200. `/preview` and `/bonus` always rehydrate by `aid` via `GET /api/analyze?aid=...`, so refresh/share/reopen can fail immediately even though the original POST reported success. - fix: make live persistence mandatory for every 200 response and return the spec'd `503 KV_WRITE_ERROR` or finalization-timeout failure instead of best-effort success.

[FATAL] src/lib/demo-fallback.ts:15 - "Use Demo Data" still retries live scraping instead of forcing fixtures - `persistDemoFixture()` only re-POSTs `/api/analyze` with a demo-looking URL, but `POST /api/analyze` enters fixture mode only when `rateLimit.forceDemoMode` is true in `src/app/api/analyze/route.ts:510`. On ordinary 422/503 failures, the advertised fallback button can fail again immediately instead of deterministically loading the Opendoor/Sono Bello fixture. - fix: add an explicit fixture-mode input/path for demo hydration and persist the selected fixture payload before navigating to `/preview`.

[MAJOR] src/app/api/analyze/route.ts:90 - Demo POSTs still reuse shared fixture IDs - `getDemoPayload()` only overrides `landingPageUrl` and `createdAt`, so the static `analysisId` values baked into `OPENDOOR_DEMO` and `SONO_BELLO_DEMO` are reused for every demo request. That means all demo runs for a given fixture overwrite the same `analysis:{id}` and `screenshot:{id}` keys, so a previously shared or refreshed `/preview?aid=demo_opendoor` link can hydrate a later request's payload instead of the exact object returned by the original POST. - fix: generate a unique demo `analysisId` per request before calling `storeDemoPayload()`, and derive the persisted screenshot URL from that per-request ID.

[MAJOR] src/lib/analyze-page.ts:1219 - `PRIMARY_FORM_UNCERTAIN` is still unreachable in the live analyze path - The scorer now implements both the `< 60` threshold and the top-two-delta rule, but `captureStep()` is still the only live call site and it always passes `{ suppressUncertain: true }`. Ambiguous multi-form pages therefore continue down the normal success/partial-success path instead of returning the required `422 PRIMARY_FORM_UNCERTAIN`. - fix: run primary-form selection once in the production scrape path without suppression, and abort the request whenever the low-score or top-two-delta rule trips.

[MAJOR] src/lib/analyze-page.ts:1040 - Post-CTA bot/challenge failures are still swallowed - `clickElementHandle()` catches every error from `waitForInteractionToSettle()`, including `AnalyzeFailure('SCRAPING_BLOCKED')`, and converts it into `false`. `navigateMultiStepJourney()` then treats that as an ordinary failed click and just stops the journey, so a challenge page that appears after the CTA can still degrade into a partial "successful" analysis instead of the required `422 SCRAPING_BLOCKED`. - fix: let typed scraper-block failures propagate out of `clickElementHandle()`, and only downgrade non-contract click errors to `false`.

[MAJOR] src/lib/analyze-page.ts:1014 - Post-click navigation failures still never normalize to `PAGE_NOT_FOUND` - `waitForNavigation()` is awaited only for timing, its `Response` is discarded, and its thrown errors are caught to `undefined` before any normalization runs. If a CTA takes the user to a missing step URL, the scraper can continue on the error page or return `NO_FORM_DETECTED`/partial success instead of surfacing the route's `422 PAGE_NOT_FOUND` contract. - fix: inspect the post-click navigation response/status and normalize click-navigation errors the same way `navigateToLandingPage()` does.

[MAJOR] src/lib/analyze-page.ts:479 - Screenshot fallback still violates the full-page contract - `takeScreenshotWithBudget()` attempts `fullPage: true`, but on any failure it silently retries with `fullPage: false` and still reports `screenshot.status: 'ok'`. That leaves a real code path where the persisted preview image is viewport-only even though the contract requires a full-page screenshot or a failed screenshot state. - fix: keep `fullPage: true` as the only success path; if that capture fails or times out, return `screenshot.status: 'failed'` and render the placeholder state instead of storing a viewport crop as success.

[MAJOR] src/app/preview/page.tsx:431 - `formBoundingBox` overlay is still hard-coded to step 1 - `analyzePage()` sends Gemini the best-scoring step screenshot (`src/lib/analyze-page.ts:1395`), but preview only renders the overlay when `activeStepData.stepNumber === 1`. On later-step forms, the left pane therefore highlights the wrong screenshot or no screenshot at all even though `formBoundingBox` exists. - fix: persist which journey step supplied the primary form and render the overlay against that step's screenshot instead of assuming step 1.

[MINOR] src/lib/demo-data.ts:129 - Later demo journey steps still reuse the step-1 PNG - Sono Bello steps 2-3 and Opendoor steps 2-4 all point their `screenshotUrl` at the same `*-step1.png` asset, so the journey navigator claims to show later pages while rendering the first screenshot repeatedly. - fix: add accurate per-step screenshot assets or leave later-step screenshots unset until those assets exist.

2 FATAL, 6 MAJOR, 1 MINOR
