[MAJOR] src/app/api/analyze/route.ts:146 - Demo screenshot URLs are still rewritten away from static `/public` assets - `storeDemoPayload()` persists demo fixtures through `persistArtifacts()`, which always calls `withPrimaryScreenshotApiUrl()`. That helper overwrites `data.screenshot.url` and `journey[0].screenshotUrl` with `/api/screenshot?id=...`, so the POST/GET payload no longer points at the checked-in `/public/*.png` files declared in `src/lib/demo-data.ts`. The demo still renders while KV is populated, but this does not satisfy the "demo screenshot assets pointing to static /public/ files" requirement and unnecessarily couples demo imagery to KV reads. - fix: preserve the original `/public/*.png` URLs for demo fixtures/timeouts and only rewrite screenshot URLs for live analyses that actually need KV-backed images.

0 FATAL, 1 MAJOR, 0 MINOR.
