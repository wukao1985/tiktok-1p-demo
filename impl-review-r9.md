[FATAL] src/lib/analyze-page.ts:1540 - Gemini failures are converted into fake live analyses - `analyzeWithGemini(...).catch(...)` still swallows every LLM timeout/auth/parse failure and substitutes placeholder brand/copy/bounding-box data while the request continues as `isSimulatedData: false`. That breaks the timeout fallback from the last round, suppresses the `503 LLM_ERROR` path, and can present fabricated "live" output to the rep. - fix: remove the blanket catch in `analyzePage()`, let non-timeout Gemini errors propagate to `mapAnalyzeError()`, and only convert real timeout sentinels into the normal simulated timeout response.
[MAJOR] src/app/preview/page.tsx:275 - Preview still hides the form overlay when the primary form is on a later journey step - hydration always initializes `activeStep` from `journey[0]`, but the overlay only renders when `activeStepData.stepNumber === data.primaryFormStepNumber` (`src/app/preview/page.tsx:431`). Any live multi-step analysis whose best form is step 2+ therefore opens without the required highlight until the user manually switches steps. - fix: initialize and restore `activeStep` from `primaryFormStepNumber`, falling back to step 1 only when that field is missing or invalid.
[MINOR] src/lib/demo-data.ts:227 - Later demo journey steps still do not have real screenshot assets - only step 1 in each fixture carries a `screenshotUrl`, so clicking step 2+ in the demo preview falls through to the generic placeholder because `activeScreenshotSrc` has nothing to render for those steps (`src/app/preview/page.tsx:426`). That leaves the "demo screenshot assets" work only partially finished. - fix: add checked-in `/public` screenshots for later demo steps and wire them via `journey[n].screenshotUrl`, or collapse the demo journey to only screenshot-backed steps.

1 FATAL, 1 MAJOR, 1 MINOR.
