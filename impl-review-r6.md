[MAJOR] src/lib/analyze-page.ts:1014 - Post-click block/not-found classification is still racy - `waitForInteractionToSettle()` races `page.waitForNavigation()` against `page.waitForNetworkIdle()` and a manual timeout, but the 401/403/429 and 404/410 normalization at `src/lib/analyze-page.ts:1036-1045` only runs when the navigation promise wins that race. If network-idle or timeout resolves first, a CTA that lands on a block/challenge page or missing page can still fall through as an ordinary step change or a no-op fingerprint break instead of returning the required `422 SCRAPING_BLOCKED` / `422 PAGE_NOT_FOUND`. - fix: capture the click and navigation together, await the navigation result deterministically, and always normalize the final response/error status before continuing the journey loop.

[MAJOR] src/app/preview/page.tsx:275 - The preview still fails to show the primary-form overlay in the shipped demo flow - after hydration, preview always initializes `activeStep` to journey step 1, while the overlay only renders when `activeStepData.stepNumber === data.primaryFormStepNumber` at `src/app/preview/page.tsx:431-437`. Both demo fixtures declare the primary form on later steps (`src/lib/demo-data.ts:125`, `src/lib/demo-data.ts:340`), but only step 1 has a screenshot asset, so the demo preview opens without any highlight and the declared primary-form step cannot render the overlay at all. - fix: initialize preview to `primaryFormStepNumber` and add matching static screenshot assets for the declared primary-form demo steps, or change the fixture metadata so the primary-form step matches the only screenshoted step.

0 FATAL, 2 MAJOR, 0 MINOR
