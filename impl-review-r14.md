[MAJOR] src/lib/analyze-page.ts:620 - PRIMARY_FORM_UNCERTAIN bypasses non-form layouts - The top-two delta rule only scores candidates returned by `field.closest('form')`. If a page uses div/section-based form markup or multiple input clusters outside literal `<form>` tags, `formCandidates` is empty, `shouldRaiseUncertain` stays false, and the code falls back to `document.body`, extracting every supported input on the page instead of returning `422 PRIMARY_FORM_UNCERTAIN`. That leaves the primary-form contract unenforced on a common class of modern landing pages. - fix: build/scored candidate containers even when no `<form>` ancestor exists (for example the nearest visible section/dialog/card with multiple controls) and apply the same `<60` and top-two-delta `<15` thresholds before any `document.body` fallback.
[MAJOR] src/lib/analyze-page.ts:410 - Bot/challenge detection can false-positive on legitimate URLs - `detectBotProtection()` treats any URL containing `captcha`, `challenge`, `blocked`, `deny`, or `verify` as blocked before requiring corroborating DOM/status evidence. `verify` and `deny` are generic substrings that can appear in normal marketing paths, so legitimate pages like `/verify-eligibility` can be mislabeled `SCRAPING_BLOCKED` even when the page content is otherwise healthy. - fix: drop the generic substring-only URL heuristic or restrict it to known challenge path patterns and require a matching DOM/title/status signal before returning `SCRAPING_BLOCKED`.

0 FATAL, 2 MAJOR, 0 MINOR.
