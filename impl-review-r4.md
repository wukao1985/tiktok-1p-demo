[MAJOR] src/app/api/analyze/route.ts:90 - Demo POSTs still reuse shared fixture IDs - `getDemoPayload()` only overrides `landingPageUrl` and `createdAt`, so the static `analysisId` values baked into `OPENDOOR_DEMO` and `SONO_BELLO_DEMO` are reused for every demo request. That means all Opendoor demo runs overwrite the same `analysis:{id}` and `screenshot:{id}` keys, so a previously shared or refreshed `/preview?aid=demo_opendoor` link can hydrate a later request's payload instead of the exact object returned by the original POST. - fix: generate a unique demo `analysisId` per request before calling `storeDemoPayload()`, and derive the persisted screenshot URL from that per-request ID.

[MAJOR] src/lib/analyze-page.ts:1219 - `PRIMARY_FORM_UNCERTAIN` is still unreachable in the live analyze path - The scoring logic now exists inside `extractFieldsFromPage()`, but `captureStep()` always calls it with `{ suppressUncertain: true }`, and no later code re-runs the primary-form check without suppression. Ambiguous multi-form pages therefore still continue as normal success/partial-success flows instead of returning the required `422 PRIMARY_FORM_UNCERTAIN`, even when the top-two delta is below 15. - fix: run primary-form selection once in the production analyze path without `suppressUncertain`, and abort the request when the low-score or top-two-delta rule trips.

[MAJOR] src/lib/analyze-page.ts:1040 - Post-CTA bot/challenge failures are still swallowed - `clickElementHandle()` catches every error from `waitForInteractionToSettle()`, including `AnalyzeFailure('SCRAPING_BLOCKED')`, and converts it into `false`. `navigateMultiStepJourney()` then treats that as an ordinary failed click and just stops the journey, so a challenge page that appears after the CTA can still degrade into a partial "successful" analysis instead of the required `422 SCRAPING_BLOCKED`. - fix: let typed scraper-block failures propagate out of `clickElementHandle()`, and only downgrade non-contract click errors to `false`.

[MAJOR] src/lib/analyze-page.ts:1014 - Post-click 404/navigation failures still never become `PAGE_NOT_FOUND` - `waitForNavigation()` is awaited only for timing and its `Response` is discarded, while navigation errors are caught to `undefined` before any status normalization runs. If a CTA takes the user to a missing step URL, the scraper can continue on the error page or return `NO_FORM_DETECTED` instead of surfacing the route's `422 PAGE_NOT_FOUND` contract. - fix: inspect the `waitForNavigation()` response/status after CTA clicks and normalize click-navigation errors the same way `navigateToLandingPage()` does.

[MAJOR] src/lib/analyze-page.ts:479 - Screenshot fallback still violates the full-page contract - `takeScreenshotWithBudget()` now attempts `fullPage: true`, but on any failure it silently retries with `fullPage: false` and still reports `screenshot.status: 'ok'`. That leaves a real code path where the persisted preview image is viewport-only, even though the spec requires a full-page screenshot or a failed screenshot state when capture cannot complete. - fix: keep `fullPage: true` as the only success path; if that capture fails or times out, return `screenshot.status: 'failed'` and render the placeholder state instead of storing a viewport crop as success.

0 FATAL, 5 MAJOR, 0 MINOR
