[FATAL] Section 5: Vertex auth/env contract is still not implemented — `src/lib/gemini.ts` no longer ships the inline API key from R1/R2, but it still ignores the spec's `GOOGLE_CLOUD_PROJECT_ID`, `GOOGLE_CLOUD_LOCATION`, and `GOOGLE_APPLICATION_CREDENTIALS_BASE64` flow, requires a non-spec `VERTEX_AI_API_KEY`, and still hardcodes a default project id. A deployment configured exactly per Section 5 will fail live analysis, and `src/app/api/analyze/route.ts` still surfaces the non-spec `AI_UNAVAILABLE` code instead of the required `LLM_ERROR`. — fix: implement the Section 5 service-account/env contract end to end, remove the hardcoded project fallback, and map missing/invalid AI credentials to `503 LLM_ERROR`.

[FATAL] Section 6: Demo hydration still bypasses KV — `src/app/api/analyze/route.ts` still returns hard-coded demo fixtures from `getDemoFixture()` when `analysis:{aid}` is missing, and `src/app/analyze/page.tsx` can still navigate straight to `demo_*` IDs via "Use Demo Data" without persisting anything first. Refresh/share can therefore succeed without hydrating the exact payload written by `POST /api/analyze`, which reopens the R3 POST→KV→GET parity failure and masks the spec's required 404/410 behavior. — fix: remove the GET fixture bypass and make every demo fallback flow persist the selected fixture before navigating to `/preview` or `/bonus`.

[MAJOR] Section 2: The 8-second budget is still internally inconsistent — `src/lib/analyze-page.ts` reserves up to 6 seconds for scraping, `src/lib/gemini.ts` still gives Gemini 5 seconds, and `src/app/api/analyze/route.ts` aborts the whole request at 8 seconds. That means normal non-error analyses can still be forced into timeout fallback before their own advertised AI budget finishes, so the R1 latency-budget issue is not actually closed. — fix: enforce one shared deadline across scraping and Gemini, with scrape time reduced enough to preserve the full 5-second AI window inside the 8-second end-to-end contract.

[MAJOR] Section 6: `KV_WRITE_ERROR` still does not trigger the required demo fallback path — `kvWriteErrorResponse()` in `src/app/api/analyze/route.ts` now returns the right `503 KV_WRITE_ERROR`, but it still omits `fallbackAvailable: true`. The loading page only reveals "Use Demo Data" when that flag is present, so a KV outage still strands the rep on an unrecoverable error state instead of the spec's required fallback UX. — fix: include `fallbackAvailable: true` on KV write failures and keep the client fallback flow identical to other 422/503 operational errors.

2 FATAL, 2 MAJOR, 0 MINOR
